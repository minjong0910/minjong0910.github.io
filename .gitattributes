# 줄바꿈을 절대 바꾸지 않는다.
#   이 PC 의 git 은 core.autocrlf=true 라서, 그대로 두면 파일을 꺼낼 때 LF 를 CRLF 로 바꾼다.
#   그러면 도구\patch_v*.ps1 의 여러 줄 앵커(LF 기준)가 index.html 과 안 맞아 모든 패치가 깨지고,
#   빌드 결과도 바이트가 달라진다. 파일은 전부 LF 로 저장되어 있다 (2026-09-21 확인).
* -text
