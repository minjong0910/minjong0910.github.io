# ── 사진 (2.9GB) — 저장소에 넣지 않는다. 목록은 batches.json 에 있다 ──
/사진원본/
/사진원본_정리대기/
/원본/
/보관/
/선택자료/
/_검토_*/
/_보류_*/
/_4층_확인필요/

# ── 빌드 결과물 — index.html 과 사진으로 언제든 다시 만든다 ──
/index_new.html
/aivec.js
/깃허브_올릴파일/
/생성물/

# ── 로컬 모델 (tfhub 모델과 값이 달라 쓰면 안 된다) ──
/model__*/
/model/

# ── 옛 백업 — 이제 git 이 대신한다 ──
/도구/백업_*.html
*.zip

# ── 실행 기록 ──
서버기록.txt

# ── 발표 PDF — 원본 HTML 에서 다시 만든다 ──
*.pdf

# ── 옛 기준벡터 — 지금 빌드는 생성물/aivec_new.json 을 읽는다 (v58~) ──
/도구/ailib_v*.json
/도구/aivec*.json

# ── 개발 도구 설정 ──
/.claude/

# ── 빌드 기록 (빌드할 때마다 한 줄) ──
빌드기록.txt
